# try:
#     result = 100/11
# except ZeroDivisionError as zero :
# 	print("0으로 나눌 수 없습니다!")
# 	print(zero)
# else :
#     print(f"결과는 '{int(result)}'입니다")
#     print(f"결과는 '{result}'입니다")
    

try:
    result = 가가가가가가
except NameError as zero :
	print("0으로 나눌 수 없습니다!")
	print(zero)
else :
    print(f"결과는 '{int(result)}'입니다")
    print(f"결과는 '{result}'입니다")